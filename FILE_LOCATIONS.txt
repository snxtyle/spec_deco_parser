===============================================================================
QUICK REFERENCE: P-EAGLE File Locations
===============================================================================

EXECUTE THESE COMMANDS (in order):
-------------------------------------------------------------------------------

# 1. Generate dataset from raw logs
python scripts/generate_data.py --local -n 1000

# 2. Extract features from target model
python -m p_eagle.scripts.extract_features \
    --model_path google/gemma-7b \
    --input_data data/output/dataset_*.jsonl \
    --output_dir data/features \
    --quantization 8bit

# 3. Train drafter model
python -m p_eagle.scripts.train_drafter \
    --drafter_model Qwen/Qwen2.5-1.5B-Instruct \
    --target_hidden_dim 3072 \
    --feature_dir data/features \
    --output_dir checkpoints \
    --epochs 50 \
    --use_lora

# 4. Run speculative inference
python -m p_eagle.scripts.run_inference \
    --target_model google/gemma-7b \
    --drafter_checkpoint checkpoints/best_model

===============================================================================
CORE SOURCE FILES
===============================================================================

p_eagle/
├── data_preparation/
│   └── data_manager.py           # EAGLEDistiller class
├── models/
│   ├── eagle_drafter.py          # EagleDrafterModel, EagleMTPHead
│   └── tree_attention.py         # TreeAttentionMask
├── training/
│   ├── feature_extractor.py      # Feature extraction logic
│   └── trainer.py                # Training loop
├── inference/
│   └── inference_engine.py       # PEAGLEInference class
└── utils/
    ├── feature_utils.py          # Dataset classes, feature fusion
    ├── loss_utils.py             # Masked MSE loss
    └── metrics.py                # MAL, speedup metrics

===============================================================================
DATA DIRECTORIES
===============================================================================

data/
├── raw/                          # Original LiteLLM logs (*.json)
├── processed/                    # Cleaned intermediate files
├── features/                     # Extracted PyTorch tensors (*.pt)
└── output/                       # Generated datasets (*.jsonl)

checkpoints/                      # Model checkpoints (created during training)
logs/                             # TensorBoard logs (created during training)

===============================================================================
CONFIGURATION
===============================================================================

.env                              # Environment variables (HF tokens, etc.)
.env.example                      # Template for .env file
requirements.txt                  # Python dependencies
setup.py                          # Package installation

See README.md for full documentation.
See PROJECT_STRUCTURE.md for detailed architecture.
===============================================================================